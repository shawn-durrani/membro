fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
anthropic>=0.40
openai>=2.52.0
# Capped at <2 deliberately: mcp 2.0 (published 2026-07-29) drops the
# `mcp.server.fastmcp` module both MCP servers here import, so an uncapped
# floor turned CI red on code that had not changed. Migrating to 2.x is its
# own reviewed change.
mcp>=1.0,<2
pytest>=8.0
httpx>=0.27
pypdf
