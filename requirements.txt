fastapi>=0.141.1
uvicorn>=0.53.0
pydantic>=2.13.5
anthropic>=1.7.0
openai>=2.54.0,<3
# The MCP SDK both MCP servers here are built on. On the 2.x line, where
# FastMCP became MCPServer. Capped at <3 because a major release can remove
# APIs these servers import, as 2.0 did, so the next major is its own
# reviewed change (#85).
mcp>=2.2.0,<3
pytest>=9.1.1
httpx>=0.28.1
pypdf
# Passkey (WebAuthn) verification for the admin UI (#27): vetted parsing and
# verification of authenticator responses; pulls in cryptography + cbor2.
# Capped at <3 like mcp above: verification semantics are security-load-bearing,
# so a major bump is its own reviewed change.
webauthn>=2.2.0,<3
